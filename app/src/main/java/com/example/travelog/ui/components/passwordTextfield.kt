package com.example.travelog.ui.components

import androidx.compose.runtime.Composable

@Composable
fun PasswordTextField(){

    // TextField(value = "", onValueChange = "", modifier = Modifier, trailingIcon = IconButton())

}
